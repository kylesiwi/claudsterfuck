---
description: Bypass all plugin routing — full unrestricted Claude turn
---

Route command.

Usage:

- `/claudsterfuck:claude <objective>` to start immediately
- `/claudsterfuck:claude` to reroute the active turn

Use when you want Claude to act freely with no worker delegation or tool enforcement.
This route is never selected automatically — only via explicit user override.
